
#abrir txt-files/teste.txt
from converter import vertex_list
from converter import text_to_json


text_to_json("txt-files/teste.txt", "test")





    

    


        

    
    
""""
    else:
        #
        a = line.split()[0]
    
        b = line.split()[1]
    #add the first item to the list and verify if it is already in the list
    #se nao for um numero inteiro, nao faz nada
        if a not in vertex_list:
            if a.isdigit():
                vertex_list.append(a)
            else:
                print("Error, not a number")
                
                break
        #add the second item to the list and verify if it is already in the list
        if b not in vertex_list:
            if b.isdigit():
                vertex_list.append(b)
            else:
                print("Error, not a number")
                break
"""
        

